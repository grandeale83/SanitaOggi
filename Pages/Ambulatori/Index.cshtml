@page
@model SanitaOggi.Pages.Ambulatori.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Ambulatorio[0].Nome)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Ambulatorio[0].NomeStruttura)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Ambulatorio[0].NomeTipo)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Ambulatorio) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomeStruttura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomeTipo)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AmbulatorioID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AmbulatorioID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AmbulatorioID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
