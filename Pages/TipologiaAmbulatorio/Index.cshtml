@page
@model SanitaOggi.Pages.TipologiaAmbulatorio.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.TipoAmbulatorio[0].CodiceTipo)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.TipoAmbulatorio[0].NomeTipo)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.TipoAmbulatorio) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CodiceTipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomeTipo)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TipoAmbulatorioID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TipoAmbulatorioID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TipoAmbulatorioID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
